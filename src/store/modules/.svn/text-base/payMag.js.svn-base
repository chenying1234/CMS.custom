const payMag = {
    namespaced: true,
    state: {
        AuditLevel: '',
    },

    mutations: {
        SET_AUDITLEVEL:(state, AuditLevel) => {
            state.AuditLevel = AuditLevel;
        },
    },

    getters: {
        AuditLevel: state => state.AuditLevel,
    }

};

export default payMag;